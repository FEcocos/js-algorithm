import { describe, test, expect } from "vitest";
import { solution } from "./solution.js";

describe("문제 제목", () => {
  test("테스트 케이스 1", () => {
    expect(solution(/* 입력값 */)).toBe(/* 예상 출력 */);
  });

  test("테스트 케이스 2", () => {
    expect(solution(/* 입력값 */)).toBe(/* 예상 출력 */);
  });

  test("엣지 케이스", () => {
    expect(solution(/* 입력값 */)).toBe(/* 예상 출력 */);
  });
});
